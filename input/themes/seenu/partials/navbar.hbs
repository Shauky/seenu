<header>
   {{! @website global variable used to display a logo and website name  }}
   {{! check if a Website logo exists, if not, dispaly the website name }}
   <div  class="navigate mt-6 flex"> 
      <a id="logo" href="{{@website.url}}">
         {{#if @website.logo}}
            <img src="{{@website.logo}}" alt="{{@website.name}}">
         {{else}}
            {{@website.name}}
         {{/if}}
      </a>
      <input type="text" class="w-2/3" placeholder="code for good">
       <div class="allnav block">
            <button class="nav-toggle">
               <i class="fa fa-bars"></i>
            </button>
            {{! check if a menu is assigned to the Main menu position }}
            <div class="linkses">
               {{#if menus.mainMenu}}
                     {{> menu menus.mainMenu}}
               {{/if}}
            </div>
            {{! /check if a menu is assigned to the Main menu position }}
      </div>
      {{! /check if a Website logo exists, if not, dispaly the website name }}
      {{! /@website global variable used to display a logo and website name  }}
   </div>
   <div class="flex justify-between">
      <small> Design and CS matters</small>
      {{!-- <button class="btn">1</button> --}}
   </div>
   {{!-- react app --}}
   <div id="root-menu">
   </div>
</header>

<style = "text/css"> 
   .linkses{
      height: 0;
      width: 0;
      overflow: hidden;
   }

   .show-links{
      height: 3rem;
      overflow: visible;
   }

   .navout{
      width: 100%;
   }

   .navigate{
      height: 36px !important;
   }

</style>